server:
  port: 9999

#  配置nacos
spring:
  application:
    name: hystrix-dashboard
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

#  将ip配置到proxyStreamAllowList
hystrix:
  dashboard:
    proxy-stream-allow-list: ["127.0.0.1"]